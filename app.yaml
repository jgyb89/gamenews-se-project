runtime: nodejs22

handlers:
  # Serve all static files (CSS, JS, Images) from the Vite dist folder
  - url: /(.*\..+)$
    static_files: dist/\1
    upload: dist/(.*\..+)$

  # Catch-all handler for React Router
  - url: /.*
    static_files: dist/index.html
    upload: dist/index.html
